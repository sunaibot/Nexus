# 生产环境配置

# API 基础地址（可选）
# - 如果设置，将优先使用此地址
# - 如果留空，系统会自动检测：
#   * 如果当前端口是 8787，使用相对路径（前后端同端口）
#   * 如果当前端口是 80/443，自动指向 :8787（NAS部署场景）
# - 如果通过 Nginx 反向代理，可以设置为空或相对路径
VITE_API_BASE=

# 应用标题（可选）
VITE_APP_TITLE=NOWEN - 智能导航
